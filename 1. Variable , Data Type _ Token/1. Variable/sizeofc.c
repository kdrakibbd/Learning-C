#include <stdio.h>

int main ()
{
    printf("\n Integer    Type date takes %d byte, \a", sizeof(int));
    printf("\n Character  Type data takes %d byte, \a", sizeof(char));
    printf("\n Float      Type data takes %d byte, \a", sizeof (float));
    printf("\n Double     Type data takes %d byte, \a", sizeof (double));

    return 0;
}
