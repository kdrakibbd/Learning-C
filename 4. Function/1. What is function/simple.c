#include <stdio.h>

int main ()
{
    int a, b, c;

    printf("Type value for b: ");
    scanf("%d", &b);

    printf("\ntype valur for c: ");
    scanf("%d", &c);

    a = b + c;

    printf("Value of a is : %d\n", a);

    return 0;
}
